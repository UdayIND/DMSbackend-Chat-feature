# Server Configuration
# Server Configuration
server.port=${SERVER_PORT:8080}

# Database Configuration
# Direct connection (with SSL and batching)
spring.datasource.url=${DB_URL:jdbc:postgresql://db.gewbkfuafhqyxpkydayb.supabase.co:5432/postgres?sslmode=require&reWriteBatchedInserts=true&connectTimeout=30}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=${DB_DRIVER:org.postgresql.Driver}

# --- HikariCP Connection Pool Settings ---
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=30000
spring.datasource.testOnBorrow=true
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.connection-timeout=30000

# Use Supabase Pooler ---
spring.datasource.url=jdbc:postgresql://postgres.gewbkfuafhqyxpkydayb:%3FDD2m48_%2BcRb4f_@aws-0-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:true}
spring.jpa.properties.hibernate.dialect=${JPA_DIALECT:org.hibernate.dialect.PostgreSQLDialect}

# WebSocket Configuration
spring.websocket.enabled=${WEBSOCKET_ENABLED:true}

# File Upload Configuration
spring.servlet.multipart.enabled=${MULTIPART_ENABLED:true}
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}
